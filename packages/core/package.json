{
  "main": "./dist/index.js",
  "name": "core",
  "scripts": {
    "build": "spago bundle --bundle-type module --module Core --outfile ./dist/index.js",
    "check": " purs-tidy check src/**/*.purs test/**/*.purs",
    "run": "spago run",
    "test": "spago test"
  },
  "type": "module"
}
